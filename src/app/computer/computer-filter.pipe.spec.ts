import { ComputerFilterPipe } from './computer-filter.pipe';

describe('ComputerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ComputerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
